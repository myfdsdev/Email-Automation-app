{
  "name": "email-automation-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "dev:worker": "node --watch ../worker/worker.js",
    "start:worker": "node ../worker/worker.js",
    "db:indexes": "node scripts/syncIndexes.js",
    "//render-build": "Render Root Directory is server/, so the build command runs here. This one script installs server + client deps and builds the SPA into ../client/dist, which app.js serves. --include=dev is required because Render sets NODE_ENV=production and Vite is a devDependency.",
    "render:build": "npm ci && npm ci --include=dev --prefix ../client && npm run build --prefix ../client"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "bcryptjs": "^2.4.3",
    "bullmq": "^5.34.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "csv-parse": "^5.6.0",
    "dayjs": "^1.11.13",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-mongo-sanitize": "^2.2.0",
    "express-rate-limit": "^7.5.0",
    "googleapis": "^144.0.0",
    "helmet": "^8.0.0",
    "ioredis": "^5.4.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.5",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.16",
    "xlsx": "^0.18.5",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "mongodb-memory-server": "^10.1.3"
  }
}
